import React from "react";
import { SinglePost, PostList, AuthorList } from "../views";
import { Layout } from ".";

export default () => {
    return (
        <>
            <SinglePost />
            <PostList />
        </>
    );
};
